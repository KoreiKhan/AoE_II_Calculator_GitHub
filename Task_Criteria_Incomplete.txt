1) Inheritance. Make at least one Entity (e.g. Archer_(Saracen)) that is inherited from another entity (e.g. Archer)

2) Polymorphism. Declare the calculator class. Take p1battleParticipant and p2battleParticipant as input run a void calculate() function

Declare calculator classes of type calculator for each round of combat with a different calculation. Run each calculation in order

3) Abstract classes. Research

4) Testing. Test 1-3 cases of the whole program. Test 1-3 cases of individual functions with Entity.h input (from your actual file). Test your actual individual functions and make sure they are by themself